{
  "scripts": {
    "postinstall": "cd frontend/native && npm install",
    "start-frontend": "cd frontend/native && npx cross-env CI=false npx expo start --tunnel --clear",
    "start-backend": "cd backend/pharmacy && python manage.py runserver",
    "start": "npm run start-frontend && npm run start-backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "cross-env": "^10.0.0"
  }
}
