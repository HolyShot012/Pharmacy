{
  "scripts": {
    "install-frontend": "cd frontend/native && npm íntall",
    "start-frontend": "cd frontend/native && npx expo start --tunnel --clear",
    "start-backend": "start cmd /k \"cd backend/pharmacy && python manage.py runserver\"",
    "start": "npm run start-frontend && npm run start-backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "cross-env": "^10.0.0"
  }
}
